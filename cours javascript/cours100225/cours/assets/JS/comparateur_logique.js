/* ---------------------------
Les opérateurs logigues
-----------------------------*/

let prenom
let loginAge
let loginPrenom
let age

// L'opérateur ET " && "

// si je cumule 2 conditions 
// si prenom === loginPrenom ET age === loginAge

if ((prenom === loginPrenom) && (age === loginAge)){

    // si ces 2 conditions sont remplis alors ici le code s'execute

}

if ((a) && (b)) {

    // ici il faut 2 conditions pour que le code s'execute sinon le code ne se lance pas
}

// l'opérateur OU " || " 

if ( a || b ){ 
    
    //ici il faut une des 2 conditions pour que le code s'execute
}

// l'operateur " ! " signifie le contraire

let userIsLogin=true
If (!userIsLogin){
    // est différent de true
    //si le useur n'est pas connecté, si useurIsLogin est false

    // ce qui revient à ecrire

}

if(userIsLogin===false){
    // si le user n'est pas connecté, si userIsLogin est false
}

